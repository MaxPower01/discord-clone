{
  "name": "discord-clone",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "install": "(npm install -g typescript) && (npm install -g gulp-cli) && (npm install -g nodemon) && (cd ../common && npm install) && (cd ../api && npm install) && (cd ../client/web && npm install) ",
    "dev": "(concurrently  \"cd common && npm run dev\" \"cd api && npm run dev\" \"cd client/web && npm run dev\" )",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MaxPower01/discord-clone.git"
  },
  "author": "Maxime Manseau",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/MaxPower01/discord-clone/issues"
  },
  "homepage": "https://github.com/MaxPower01/discord-clone#readme",
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}